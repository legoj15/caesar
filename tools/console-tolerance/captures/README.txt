# Optional in-tree capture location.
#
# By default console-tolerance.ps1 looks for the console captures in the
# extracted MeetSound bank directory (...\MiiPlaza\sound\MeetSound\BANK_BGM\),
# where the 2026-07-08 captures already live. To keep captures here instead,
# pass -CapturesDir pointing at this folder:
#
#   tools\console-tolerance\console-tolerance.ps1 -CapturesDir tools\console-tolerance\captures
#
# Drop the WAVs named in docs\CAPTURE-REQUEST.md here. WAVs are gitignored.
